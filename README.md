## Setup Instructions

1. Clone the repository
2. Create a file called `gradle.properties` in the root folder
3. Add the following line:
   GEMINI_API_KEY=your_gemini_api_key_here
4. Go to File → Sync Project with Gradle Files
5. Run the app
